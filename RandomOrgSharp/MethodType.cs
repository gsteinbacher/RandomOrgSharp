namespace Obacher.RandomOrgSharp.Core
{
    public enum MethodType
    {
        Integer,
        Decimal,
        Gaussian,
        String,
        Uuid,
        Blob,
        Usage,
        VerifySignature
    }
}
