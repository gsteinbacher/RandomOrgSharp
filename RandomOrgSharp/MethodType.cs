namespace Obacher.RandomOrgSharp
{
    internal enum MethodType
    {
        Integer,
        Decimal,
        Gaussian,
        String,
        UUID,
        Blob,
        Usage,
        SignedInteger,
        SignedDecimal,
        SignedGaussian,
        SignedString,
        SignedUUID,
        SignedBlob,
        VerifySignature
    }

}
